extends layouts/main
include mixins/videoBlcok

block content
    .search__header 
        h1 Search for #{searchingBy}
    .search__videos
        if videos.length === 0
            h5 No Vidoes Found
        each video in videos 
            +videoBlock({
                title: video.title,
                views: video.views,
                videoFile:video.fileUrl,
                id: video.id
            })
            +videoBlock({
                title: video.title,
                views: video.views,
                videoFile:video.fileUrl,
                id: video.id
            })
            +videoBlock({
                title: video.title,
                views: video.views,
                videoFile:video.fileUrl,
                id: video.id
            })
            +videoBlock({
                title: video.title,
                views: video.views,
                videoFile:video.fileUrl,
                id: video.id
            })
            